{
  "name": "migrate-legacy-data",
  "version": "1.0.0",
  "private": true,
  "description": "One-off script to move data from the old Realtime Database app into the new Firestore schema.",
  "main": "migrate.js",
  "scripts": {
    "migrate": "node migrate.js",
    "migrate:dry-run": "node migrate.js --dry-run"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0"
  }
}
